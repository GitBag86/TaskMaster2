{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "preDeployCommand": "python -m flask --app app db upgrade && python scripts/seed_production.py"
  }
}
