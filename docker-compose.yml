services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
    env_file:
      - .env
    environment:
      - FLASK_APP=app.py
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
